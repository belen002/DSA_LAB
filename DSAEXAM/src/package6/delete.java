package package6;

public class delete {
    public class Arrays {
        void array(int size) {
            int[] newarray = new int[size];

        }
    }

    public int deleteElement(int[] numbers, int index) {
        int i;
        if (numbers[i] == numbers[index]) {
            return numbers[i];
        } else if (numbers[index] <= numbers.length && numbers[index] > numbers.length) {
            System.out.println("invalid index"
            );
        }
    }
}
}
